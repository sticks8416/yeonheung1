<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="seigyoDao">

<select id="seigyolist" resultType="map">
		SELECT ryhntn_max_length, shhn_max_length, max_order_number FROM 制御マスタ
	</select>
	</mapper>